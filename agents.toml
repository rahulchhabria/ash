version = 1
# Managed skills are gitignored.
gitignore = true
# Skills always fetch the latest version; lockfile tracks names only.
pin = false
agents = ["claude", "cursor", "codex"]

[trust]
allow_all = true

[[skills]]
name = "*"
source = "dcramer/dex"
